# requirements.txt
# ================================================================
# CORE runtime only (aligned with pyproject [project.dependencies])
# ================================================================

numpy>=1.24.0,<3.0.0
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0
scipy>=1.11.0,<2.0.0
ta>=0.10.0,<1.0.0
requests>=2.31.0,<3.0.0
psutil>=6.0.0,<7.0.0
cryptography>=42.0.0
tqdm>=4.65.0,<5.0.0

# Optional install profiles:
# - Desktop/UI + CN feeds + ML:  pip install -r requirements-desktop.txt
# - Full optional stack:          pip install -r requirements-all.txt
